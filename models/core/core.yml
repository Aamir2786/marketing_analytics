version: 2

models:
  - name: dim_account
    description: Account dimension table
    columns:
      - name: account_id
        description: Primary key
        tests:
          - not_null
          - unique
  - name: dim_users
    description: User dimension table
    columns:
      - name: lead_id
        description: Primary key
        tests:
          - not_null
          - unique